namespace Setup
{
    /// <summary>
    /// permissions available for an assembly dll in sql server
    /// </summary>
    public enum PermissionSetType
    {
        SAFE,
        EXTERNAL_ACCESS,
        UNSAFE
    };
}
